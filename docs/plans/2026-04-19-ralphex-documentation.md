# План подготовки документа по ralphex

1. Подготовить отдельный markdown-документ в `docs/` с разбором `umputun/ralphex`: как снижать расход токенов, как комбинировать модели и `effort`.
2. Добавить в тот же документ три готовых профиля конфигурации `ralphex`: `cheap`, `balanced`, `strict`, с кратким пояснением, когда какой профиль использовать.
3. Проверить созданные файлы и рабочее дерево, чтобы убедиться, что документ сохранён в нужное место и не содержит явных структурных ошибок.
